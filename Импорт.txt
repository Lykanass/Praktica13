
&НаСервере
Процедура ИмпортДанныхНаСервере(ИмяФайла)
	    Текст = Новый ТекстовыйДокумент();
		Текст.Прочитать(ИмяФайла);

		КоличествоСтрок = Текст.КоличествоСтрок();

	Для Счетчик = 7 по КоличествоСтрок Цикл
		ТекСтрока = Текст.ПолучитьСтроку(Счетчик);

		МассивСтр = СтрРазделить(ТекСтрока,";");

		ИнфНос = Справочники.Автомобили.СоздатьЭлемент();

		ИнфНос = Справочники.Автомобили.СоздатьЭлемент();
		ИнфНос.Код = МассивСтр[0];
		ИнфНос.Наименование = МассивСтр[1];
		ИнфНос.Цена = МассивСтр[2];
		ИнфНос.Класс = МассивСтр[3];
		ИнфНос.Цвет = МассивСтр[4];
		ИнфНос.Мощность = МассивСтр[5];

		ИнфНос.Записать();
	КонецЦикла;
	Сообщить("Мазель тов!");
КонецПроцедуры



&НаКлиенте
Процедура Импорт(Команда)
	ДиалогВыбора = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);

	ДиалогВыбора.Заголовок = "Выберите файл";


	Если ДиалогВыбора.Выбрать() Тогда

	ИмяФайла = ДиалогВыбора.ПолноеИмяФайла;
	КонецЕсли;
	ИмпортДанныхНаСервере(ИмяФайла)
	
КонецПроцедуры






	